import { MASK } from './constants';
export function hammingWeight(num) {
    num = num - ((num >> 1) & 0x55555555);
    num = (num & 0x33333333) + ((num >> 2) & 0x33333333);
    return ((num + (num >> 4) & 0xF0F0F0F) * 0x1010101) >> 24;
}
export function hashFragment(shift, hash) {
    return (hash >>> shift) & MASK;
}
export function toBitmap(num) {
    return 1 << num;
}
export function bitmapToIndex(shift, bitmap) {
    return hammingWeight(shift & (bitmap - 1));
}
//# sourceMappingURL=bitwise-operations.js.map