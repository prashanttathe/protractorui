import { Node, NodeType, Leaf } from '../types';
export declare class LeafNode<K, V> implements Leaf<K, V> {
    type: NodeType.LEAF;
    hash: number;
    key: K;
    value: V;
    constructor(hash: number, key: K, value: V);
    modify(shift: number, get: (value?: V) => V, hash: number, key: K, size: {
        value: number;
    }): Node<K, V>;
}
