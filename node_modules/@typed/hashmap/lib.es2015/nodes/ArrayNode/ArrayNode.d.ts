import { NodeType, ListNode, Node } from '../types';
export declare class ArrayNode<K, V> implements ListNode<K, V> {
    type: NodeType.ARRAY;
    size: number;
    children: Array<Node<K, V>>;
    constructor(size: number, children: Array<Node<K, V>>);
    modify(shift: number, get: (value?: V) => V, hash: number, key: K, size: {
        value: number;
    }): Node<K, V>;
}
