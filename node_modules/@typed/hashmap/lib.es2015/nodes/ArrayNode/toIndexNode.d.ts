import { Node } from '../types';
import { IndexedNode } from '../IndexedNode';
export declare function toIndexNode<K, V>(count: number, index: number, children: Array<Node<K, V>>): IndexedNode<K, V>;
