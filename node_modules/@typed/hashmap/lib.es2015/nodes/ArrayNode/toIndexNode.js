import { IndexedNode } from '../IndexedNode';
export function toIndexNode(count, index, children) {
    var newChildren = new Array(count - 1);
    var g = 0;
    var bitmap = 0;
    for (var i = 0; i < children.length; ++i) {
        if (i !== index) {
            var child = children[i];
            if (child && child.type > 0 /* EMPTY */) {
                newChildren[g++] = child;
                bitmap |= 1 << i;
            }
        }
    }
    return new IndexedNode(bitmap, newChildren);
}
//# sourceMappingURL=toIndexNode.js.map