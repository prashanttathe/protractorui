import { Collision, NodeType, Node } from '../types';
import { LeafNode } from '../LeafNode';
export declare class CollisionNode<K, V> implements Collision<K, V> {
    type: NodeType.COLLISION;
    hash: number;
    children: Array<LeafNode<K, V>>;
    constructor(hash: number, children: Array<LeafNode<K, V>>);
    modify(shift: number, get: (value?: V) => V, hash: number, key: K, size: {
        value: number;
    }): Node<K, V>;
}
