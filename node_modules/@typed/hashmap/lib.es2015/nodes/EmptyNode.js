import { NOTHING } from './constants';
import { LeafNode } from './LeafNode';
var EmptyNode = (function () {
    function EmptyNode() {
        this.type = 0 /* EMPTY */;
    }
    EmptyNode.prototype.modify = function (shift, get, hash, key, size) {
        var value = get(void shift);
        if (value === NOTHING)
            return this;
        ++size.value;
        return new LeafNode(hash, key, value);
    };
    return EmptyNode;
}());
export { EmptyNode };
export var EMPTY = new EmptyNode();
export function empty() {
    return EMPTY;
}
//# sourceMappingURL=EmptyNode.js.map