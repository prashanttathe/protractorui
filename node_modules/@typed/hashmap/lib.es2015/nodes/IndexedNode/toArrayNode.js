import { ArrayNode } from '../ArrayNode';
export function toArrayNode(fragment, child, bitmap, children) {
    var array = [];
    var bit = bitmap;
    var count = 0;
    for (var i = 0; bit; ++i) {
        if (bit & 1)
            array[i] = children[count++];
        bit >>>= 1;
    }
    array[fragment] = child;
    return new ArrayNode(count + 1, array);
}
//# sourceMappingURL=toArrayNode.js.map