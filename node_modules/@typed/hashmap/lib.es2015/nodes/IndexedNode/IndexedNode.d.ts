import { Indexed, Node, NodeType, ChildrenNodes } from '../types';
export declare class IndexedNode<K, V> implements Indexed<K, V> {
    type: NodeType.INDEX;
    mask: number;
    children: ChildrenNodes<K, V>;
    constructor(mask: number, children: ChildrenNodes<K, V>);
    modify(shift: number, get: (value?: V) => V, hash: number, key: K, size: {
        value: number;
    }): Node<K, V>;
}
