import { Node } from '../nodes';
export declare class HashMap<K, V> implements Iterable<[K, V]> {
    private node;
    private size;
    constructor(node: Node<K, V>, size: number);
    [Symbol.iterator](): Iterator<[K, V]>;
}
