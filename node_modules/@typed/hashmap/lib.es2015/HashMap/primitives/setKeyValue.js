import { findHash } from './findHash';
import { getNode } from './getNode';
import { getSize } from './getSize';
import { setTree } from './setTree';
var constant = function (x) { return function () { return x; }; };
export function setKeyValue(key, value, map) {
    var hash = findHash(key);
    var size = { value: getSize(map) };
    var newNode = getNode(map).modify(0, constant(value), hash, key, size);
    return setTree(newNode, size.value, map);
}
//# sourceMappingURL=setKeyValue.js.map