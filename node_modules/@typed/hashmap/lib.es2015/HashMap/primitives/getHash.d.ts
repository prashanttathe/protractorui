import { HashMap } from './../HashMap';
/**
 * Tries to find the value of a hash and key in a HashMap
 */
export declare function getHash<K, V, R>(defaultValue: R, hash: number, key: K, map: HashMap<K, V>): V | R;
