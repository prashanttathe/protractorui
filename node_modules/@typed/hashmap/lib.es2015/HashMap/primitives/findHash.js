import { stringHash } from '../../common';
export function findHash(key) {
    var hash = typeof key === 'number'
        ? key
        : typeof key === 'string'
            ? stringHash(key)
            : Math.abs(stringHash(JSON.stringify(key)));
    return hash;
}
//# sourceMappingURL=findHash.js.map