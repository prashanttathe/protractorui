import { HashMap } from './HashMap';
export declare const reduce: ReduceFn;
export interface ReduceFn {
    <K, V, R>(f: (accum: R, value: V, key?: K) => R, seed: R, map: HashMap<K, V>): R;
    <K, V, R>(f: (accum: R, value: V, key?: K) => R): (seed: R, map: HashMap<K, V>) => R;
    <K, V, R>(f: (accum: R, value: V, key?: K) => R, seed: R): (map: HashMap<K, V>) => R;
    <K, V, R>(f: (accum: R, value: V, key?: K) => R): (seed: R) => (map: HashMap<K, V>) => R;
}
